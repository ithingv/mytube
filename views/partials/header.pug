header.header
     .header__wrapper
        .header__column
            a(href=routes.home)
                i.fab.fa-youtube
        .header__column
            form(action=routes.search, method="get")
                input(type="text", placeholder="検索してください", name="term")
        .header__column
            ul
                if !user
                    li
                        a(href=routes.join) Join
                    li
                        a(href=routes.login) Log In
                else 
                    li
                        a(href=`/videos${routes.upload}`) アップロード
                    li
                        a(href=routes.userDetail(user.id)) プロフィール
                    li
                        a(href=routes.logout) ログアウト