extends layouts/main

block content
    .form-container
        form(action=routes.join,method="post")
            input(type="text", name="name", placeholder="Full Name".required=true)
            input(type="email", name="email", placeholder ="Email",required=true)
required=true            input(type="password", name="password", placeholder="Full Name",required=true)
required=true            input(type="password", name="password2", placeholder="Verify Name",required=true)        
            input(type="submit", value = "Join now")
        include partials/socialLogin